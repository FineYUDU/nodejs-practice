
// console.log( process.env );

const { SHELL, USERNAME } = process.env;

// console.table( {SHELL, USERNAME} );

const characters = [ 'Batman', 'Robin', 'Joker', 'Green Lanter' ];

const [b,r,j,gl] = characters;

// console.log(gl);